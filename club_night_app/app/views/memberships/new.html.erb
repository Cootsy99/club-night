<% if flash[:error] %>
  <div class="alert alert-danger">
    <%= flash[:error] %>
  </div>
<% end %>

<h1>New Membership for <%=params[:club_name]%> </h1>


<%if @club_memberships == 0 %>
<div>
<p>🙌 You are the club's first member and therefore an admin 🙌</p>
</div>
<%end%>
<%= render "form", membership: @membership %>

<br>

<div>
  <%= link_to("Back to My Profile", user_path(current_user), class: "btn btn-info") %>
</div>
<br>
<br>

<%# <div>
  <%= link_to("Back to #{params[:club_name]}", club_path(params[:club_id]), class: "btn btn-info") %>
<%# </div> %> 