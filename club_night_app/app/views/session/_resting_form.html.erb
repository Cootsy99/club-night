
<div class = "resting-form-container">
<form action="<%= session_need_rest_path %>" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :membership, membership %>
  <%= hidden_field_tag :club_id, club_id %>

  <button type="submit" class="btn btn-outline-warning">Needs a rest 💤</button>
</form>
</div>