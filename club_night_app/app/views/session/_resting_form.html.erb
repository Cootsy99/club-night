
<div class = "resting-form-container">
<form action="<%= session_update_resting_path %>" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :membership, membership %>
  <%= hidden_field_tag :club_id, club_id %>

  <button type="submit" class="btn btn-primary">Needs a rest</button>
</form>
</div>